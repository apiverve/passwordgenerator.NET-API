<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>netstandard2.1</TargetFramework>
    <Nullable>enable</Nullable>
    <GeneratePackageOnBuild>True</GeneratePackageOnBuild>
    <Title>APIVerve.API.PasswordGenerator</Title>
    <Version>1.0.0</Version>
    <Authors>APIVerve</Authors>
    <Company>APIVerve</Company>
    <PackageLicenseFile>LICENSE</PackageLicenseFile>
    <PackageRequireLicenseAcceptance>False</PackageRequireLicenseAcceptance>
    <PackageReadmeFile>Readme.md</PackageReadmeFile>
    <PackageProjectUrl>https://apiverve.com/marketplace/api/passwordgenerator</PackageProjectUrl>
    <SignAssembly>False</SignAssembly>
    <Description>Password Generator is a simple tool for generating passwords. It returns the generated password.</Description>
    <RepositoryUrl>https://github.com/apiverve/passwordgenerator-NET</RepositoryUrl>
    <RepositoryType>git</RepositoryType>
    <PackageTags>password generator; password generation; password generator api; password generation api; password generator tool; password generation tool; password generator software; password generation software; password generator service; password generation service; password generator library; password generation library; password generator sdk; password generation sdk; password generator code; password generation code; password generator script; password generation script; password generator program; password generation program; password generator application; password generation application; password generator app; password generation app; password generator utility; password generation utility; password generator helper; password generation helper; password generator helper tool; password generation helper tool; password generator helper software; password generation helper software; password generator helper service; password generation helper service; password generator helper library; password generation helper library; password generator helper sdk; password generation helper sdk; password generator helper code; password generation helper code; password generator helper script; password generation helper script; password generator helper program; password generation helper program; password generator helper application; password generation helper application; password generator helper app; password generation helper app; password generator helper tool; password generation helper tool; password generator helper utility; password generation helper utility; password generator helper</PackageTags>
    <Copyright>©2024 APIVerve. All Rights Reserved</Copyright>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Newtonsoft.Json" Version="13.0.3" />
  </ItemGroup>

  <ItemGroup>
    <None Update="LICENSE">
      <Pack>True</Pack>
      <PackagePath>\</PackagePath>
    </None>
    <None Update="Readme.md">
      <Pack>True</Pack>
      <PackagePath>\</PackagePath>
    </None>
  </ItemGroup>

</Project>
